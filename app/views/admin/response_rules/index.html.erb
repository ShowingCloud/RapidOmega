<p id="notice"><%= notice %></p>
<table class="table">
  <thead>
    <tr>
      <th>回复规则</th>
      <th>回复类型</th>
      <th>回复内容</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @response_rules.each do |rr| %>
      <tr>
        <td><%= rr.rule.name %></td>
        <td><%= I18n.t("msgtype.#{rr.response.msgtype}") %></td>
        <td><%= rr.response.message %></td>
        <td><%= link_to '编辑', edit_admin_response_rule_path(rr) %></td>
        <td><%= link_to '删除', admin_response_rule_path(rr), method: :delete, data: { confirm: '确定删除?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '新建', new_admin_response_rule_path %>
