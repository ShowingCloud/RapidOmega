<h2>菜单编辑器</h2>
<p>1、自定义菜单最多包括3个一级菜单，每个一级菜单最多包含5个二级菜单。<br>
2、一级菜单最多4个汉字，二级菜单最多7个汉字，多出来的部分将会以“...”代替</p>
<div class="row">
  <div id="mediaModal" class="modal fade" role="dialog">
  <div class="modal-dialog modal-lg">

    <!-- Modal content-->
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h4 class="modal-title">选择图文</h4>
      </div>
      <div class="modal-body">
        <div class="container-fluid">
          <div class="row">
          </div>
        </div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-default" data-dismiss="modal">关闭</button>
      </div>
    </div>

  </div>
</div>
  <div class="col-md-4">
  <div class="menu-preview">
    <nav class="bottom">
      <div class="container-fluid">
        <ul class="nav navbar-nav level1">
          <% @menu["menu"]["button"].each do |bt| %>
            <% if bt["sub_button"].length > 0 %>
              <li class="item dropup open">
                <a><%= bt["name"] %>
                <span class="caret"></span></a>
                <ul class="dropdown-menu leve2">
                <% bt["sub_button"].each do |sbt| %>
                  <li class="item"><a data-type="<%= sbt["type"] if sbt["type"] %>" data-url="<%= sbt["url"] if sbt["url"] %>" data-media_id="<%= sbt["media_id"] if sbt["media_id"] %>" ><%= sbt["name"] %></a></li>
                <% end %>
                </ul>
              </li>
            <% else %>
              <li class="item"><a data-type="<%= bt["type"] if bt["type"] %>" data-url="<%= bt["url"] if bt["url"] %>" data-media_id="<%= bt["media_id"] if bt["media_id"] %>" ><%= bt["name"] %></a></li>
              <ul class="dropdown-menu leve2 hidden"></ul>
            <% end %>
          <% end %>
        </ul>
      </div>
    </nav>
  </div>
</div>
<div class="col-md-6 menu-editor">
  <div class="box box-info">
            <div class="box-header with-border">
              <h3 class="box-title">菜单</h3>
              <button type="button" id="done" class="btn btn-info pull-right">提交</button>
            </div>
            <!-- /.box-header -->
            <!-- form start -->
            <form class="form-horizontal">
              <div class="box-body">
                <div class="form-group">
                  <label for="menuName" class="col-sm-3 control-label">名称</label>

                  <div class="col-sm-9">
                    <input type="text" class="form-control" id="menuName" placeholder="菜单名称">
                  </div>
                </div>

                <div class="form-group">
                <label class="col-sm-3 control-label">类型</label>
                <div class="col-sm-9">
                  <select class="form-control" id="menuType">
                    <option value="view">跳转网页链接</option>
                    <option value="media_id">下发媒体消息</option>
                    <option value="">普通按钮</option>
                  </select>
                </div>

              </div>
                <div class="form-group hidden">
                  <label for="menuUrl" class="col-sm-3 control-label">链接</label>
                  <div class="col-sm-9">
                    <input type="url" class="form-control" id="menuUrl" placeholder="网页链接">
                  </div>
                </div>
                <div class="form-group hidden">
                  <label for="menuUrl" class="col-sm-3 control-label">媒体消息</label>
                  <div class="col-sm-9">
                    <a class="btn btn-primary" id="getMediaNews">选择图文</a>
                    <input type="hidden" class="form-control" id="menuMedia">
                    <div id="selected_media"></div>
                  </div>
                </div>
              </div>
              <!-- /.box-body -->
              <div class="box-footer">
                <button type="button" id="save" class="btn btn-primary">确认</button>
                <button type="button" id="delete" class="btn btn-danger pull-right">删除</button>
              </div>
              <!-- /.box-footer -->
            </form>
          </div>
</div>
</div>
